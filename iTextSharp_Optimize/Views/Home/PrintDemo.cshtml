@*@using iTextSharp_Optimize.Models

    @model global::iTextSharp_Optimize.Models.CustomerList
        @{
            var counter = 1;
        }
    <div class="col-md-6">
        <a href="#" class="label label-success"><span class="glyphicon glyphicon-user"></span>  User</a>
    </div>
    <div class="col-md-6">
        <table class="table table-striped" cellpadding="4" cellspacing="4">
            <thead class="text-center">
                <tr>
                    <td>#</td>
                    <td>Name</td>
                    <td>Address</td>
                    <td>Place</td>
                </tr>
            </thead>
            <tbody>
                @foreach (Customer customer in Model)
                {
                    <tr>
                        <td>@counter</td>

                        <td>@customer.Name</td>

                        <td>@customer.Address</td>

                        <td>@customer.Place</td>

                    </tr>
                }
            </tbody>
        </table>
    </div>*@


@using iTextSharp_Optimize.Models

@model iTextSharp_Optimize.Models.CustomerList
<br />
<img src="@Model.ImageUrl" width="64" height="64" />
<br />
<table cellpadding="3" cellspacing="3">
    <tr border="1" bgcolor="#777777" color="#ffffff">
        <td width="20%" align="center">
            Name
        </td>
        <td width="50%" align="center">
            Address
        </td>
        <td width="30%" align="center">
            Place
        </td>
    </tr>
    @{
        var OddColor = "#DDEEEE";
        var evenColor = "#EEFFFF";
        var Odd = OddColor;
    }
    @foreach (Customer customer in Model)
    {
        <tr border="1" bgcolor="@Odd">
            <td>
                @customer.Name
        </td>
        <td>
            @customer.Address
    </td>
    <td>
        @customer.Place
</td>
</tr>
        if (Odd == OddColor)
        {
            Odd = evenColor;
        }
        else
        {
            Odd = OddColor;
        }

    }
</table>
